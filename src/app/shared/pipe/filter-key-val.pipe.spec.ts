import { FilterKeyValPipe } from './filter-key-val.pipe';

describe('FilterKeyValPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKeyValPipe();
    expect(pipe).toBeTruthy();
  });
});
