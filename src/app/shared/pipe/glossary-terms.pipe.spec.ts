import { GlossaryTermsPipe } from './glossary-terms.pipe';

describe('GlossaryTermsPipe', () => {
  it('create an instance', () => {
    // const pipe = new GlossaryTermsPipe();
    // expect(pipe).toBeTruthy();
  });
});
