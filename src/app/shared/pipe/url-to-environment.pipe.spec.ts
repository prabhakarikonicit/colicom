import { UrlToEnvironmentPipe } from './url-to-environment.pipe';

describe('UrlToEnvironmentPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlToEnvironmentPipe();
    expect(pipe).toBeTruthy();
  });
});
